/**
 * [CSS3 loading模块]
 */
define(function( require, exports ){
	var $ = require('jquery');

	var Loading = {
		// config配置参数{type: xxx, width: 100%, height: auto, align: center}
		init: function( config ) {
			this.$config = config;
			switch( config.type ) {
				case ''
			}
		}
	}
	exports.base = Loading;
});